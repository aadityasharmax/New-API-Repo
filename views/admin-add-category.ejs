<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Add Category</title>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-6">

  <div class="bg-zinc-800 shadow-lg rounded-xl p-8 w-full max-w-md">
    <h1 class="text-2xl font-bold text-center mb-6 text-blue-400">Add New Category</h1>

    <form action="/admin/category/add" method="POST" enctype="multipart/form-data" class="space-y-5">
      <div>
        <label for="name" class="block mb-2 text-gray-300">Category Name:</label>
        <input 
          type="text" 
          id="name" 
          name="name" 
          required
          class="w-full px-4 py-2 bg-zinc-900 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-white placeholder-gray-400"
          placeholder="Enter category name">
      </div>

      <div class="mb-4">
    <label for="name" class="block mb-2 text-gray-300">Category Image</label>
    <input type="file" name="image" accept="image/*" required>
  </div>

      <button 
        type="submit" 
        class="w-full py-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-lg transition duration-200">
        Add Category
      </button>
    </form>

    
    <div class="text-center mt-6">
      <a href="/admin/categories" class="text-blue-400 hover:underline">← Back to Categories</a>
    </div>
  </div>

</body>
</html>
