<!DOCTYPE html>
<html>
<head>
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white p-10">
  <h1 class="text-2xl mb-4">Edit Post</h1>
  <form action="/admin/post/edit/<%= post._id %>" method="POST" enctype="multipart/form-data" class="space-y-4">
    <input type="text" name="title" value="<%= post.title %>" class="w-full px-4 py-2 text-black rounded" required>
    <textarea name="description" class="w-full px-4 py-2 text-black rounded" required><%= post.description %></textarea>
    <input type="file" name="image" class="w-full">
    <% if(post.image) { %>
      <img src="<%= post.image %>" class="w-32 h-32 object-cover mt-2">
    <% } %>
    <button class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white">Update Post</button>
  </form>
</body>
</html>
