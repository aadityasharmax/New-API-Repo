<!DOCTYPE html>
<html>
<head>
  <title><%= post.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white p-6">
  <div class="max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold mb-4"><%= post.title %></h1>
    <p class="text-gray-400 mb-2">By <%= post.authorName %> | <%= post.createdAt.toDateString() %></p>

    <% if (post.image) { %>
      <img class="h-20" src="<%= post.image %>" alt="Post Image" class="rounded-lg mb-6 w-full">
    <% } %>

    <p class="text-lg leading-relaxed"><%= post.description %></p>

    <div class="mt-8">
      <a href="/" class="text-blue-400 hover:underline">← Back to Dashboard</a>
    </div>
  </div>
</body>
</html>