<!DOCTYPE html>
<html>
<head>
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white p-10">
  <h1 class="text-2xl mb-4">Edit User</h1>
  <form action="/admin/edit/<%= user._id %>" method="POST" class="space-y-4">
    <div>
      <label class="block mb-2 text-gray-400">Username</label>
      <input type="text" name="username" value="<%= user.username %>"
             class="w-full p-2 text-black rounded" required>
    </div>
    <div>
      <label class="block mb-2 text-gray-400">Email</label>
      <input type="email" name="email" value="<%= user.email %>"
             class="w-full p-2 text-black rounded" required>
    </div>
    <div>
      <label class="block mb-2 text-gray-400">Age</label>
      <input type="number" name="age" maxlength="3" value="<%= user.age %>"
             class="w-full p-2 text-black rounded" required>
    </div>
    <button class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white">Update User</button>
    <a href="/admin" class="ml-4 text-gray-400 hover:underline">Cancel</a>
  </form>
</body>
</html>
